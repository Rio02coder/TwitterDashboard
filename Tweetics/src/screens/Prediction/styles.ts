import {StyleSheet} from 'react-native';

export const fluPredictionScreenStyles = StyleSheet.create({
  headerText: {
    alignSelf: 'center',
    color: 'white',
    fontSize: 30,
    fontWeight: 'bold',
    marginVertical: '8%',
  },
});
